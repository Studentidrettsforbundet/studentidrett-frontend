import { style, classes } from 'typestyle';
import colors from '../../styles/colors';

export const coverImage = style({
    maxWidth: '100%',
    height: 'auto',
    marginRight: 'auto',
    marginLeft: 'auto',
    display: 'block'
})